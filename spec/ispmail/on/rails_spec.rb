require 'spec_helper'

describe Ispmail::On::Rails do
  it 'has a version number' do
    expect(Ispmail::On::Rails::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
